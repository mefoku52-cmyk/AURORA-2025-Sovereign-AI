#!/usr/bin/env bash
LOG_FILE="$HOME/aurora_dashboard.log"
echo "*** NET DIAGNOSTIKA START ***" >> "$LOG_FILE"
if ping -c 1 -W 1 google.com >/dev/null 2>&1; then
  echo "Sieť OK" | tee -a "$LOG_FILE"
else
  echo "Sieť izolovaná" | tee -a "$LOG_FILE"
fi
